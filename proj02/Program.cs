using System;


namespace proj02
{
    class Program
    {
        static void Main(string[] args)
        {
            // Exemplo de uso das classes

            // Criando um nível de usuário com suas permissões
            // NivelAcesso nivel = new NivelAcesso("Administrador", new List<string> { "Criar usuário", "Editar usuário", "Excluir usuário" });

            // Criando um usuário com o nível de acesso definido
            // Usuario usuario = new Usuario("Fulano", "fulano@example.com", "senha123", nivel, new DateTime(1990, 5, 11), "Rua A, 123", "123456789");

            // Realizando o cadastro do usuário
            // usuario.Cadastro();

            // Realizando o login do usuário
            // usuario.Login();

            /*NotaFiscal nf = new NotaFiscal("alface", 1, "Giovana", "123");
            Console.WriteLine(nf);*/
        }
    }
    
}